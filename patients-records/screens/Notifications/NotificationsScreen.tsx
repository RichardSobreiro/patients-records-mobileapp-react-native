import NotificationsHome from '../../components/notifications/NotificationsHome';

const NotificationsScreen = ({ route, navigation }) => {
  return <NotificationsHome navigation={navigation} />;
};

export default NotificationsScreen;
